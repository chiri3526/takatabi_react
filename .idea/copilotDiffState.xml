<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/scripts/ensure_cms_image_patch_all.js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/scripts/ensure_cms_image_patch_all.js" />
              <option name="updatedContent" value="const fs = require('fs');&#10;const path = require('path');&#10;const file = path.join(__dirname, '..', 'src', 'pages', 'ArticlePage.js');&#10;let s = fs.readFileSync(file, 'utf8');&#10;// Replace max-width for cms-image occurrences&#10;s = s.replace(/max-width:\s*\d+px\s*!important;/g, 'max-width: 920px !important;');&#10;// Replace background and padding when used in context of cms-image block&#10;s = s.replace(/(img\.cms-image,\n\s*\.cms-image\s*\{[\s\S]*?)(background:\s*#fff;)/g, (m, p1) =&gt; p1.replace(/background:\s*#fff;/g, 'background: transparent;'));&#10;// generic replace for padding: 8px; near cms-image&#10;s = s.replace(/(img\.cms-image,\n\s*\.cms-image\s*\{[\s\S]*?)(padding:\s*8px;)/g, (m, p1) =&gt; p1.replace(/padding:\s*8px;/g, 'padding: 0;'));&#10;// Also fallback: replace any standalone padding: 8px; following .cms-image&#10;s = s.replace(/(\.cms-image[^\{]*\{[\s\S]*?)padding:\s*8px;/g, (m) =&gt; m.replace(/padding:\s*8px;/, 'padding: 0;'));&#10;// write back&#10;fs.writeFileSync(file, s, 'utf8');&#10;console.log('Ensured cms-image CSS patched across file');&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/scripts/patch_cms_image_css.js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/scripts/patch_cms_image_css.js" />
              <option name="updatedContent" value="const fs = require('fs');&#10;const path = require('path');&#10;const file = path.join(__dirname, '..', 'src', 'pages', 'ArticlePage.js');&#10;let s = fs.readFileSync(file, 'utf8');&#10;const oldBlockStart = '  /* 本文内画像をコンテナ内に収める */\n  img.cms-image,';&#10;const idx = s.indexOf(oldBlockStart);&#10;if (idx === -1) { console.error('old block not found'); process.exit(1); }&#10;// find end of the block by finding the next closing brace of that selector group&#10;const from = idx;&#10;const braceIdx = s.indexOf('\n\n', from);&#10;// We'll do a targeted replace of localized properties: max-width, background, padding&#10;s = s.replace(/max-width:\s*\d+px\s*!important;/g, 'max-width: 920px !important;');&#10;s = s.replace(/background:\s*#fff;/g, 'background: transparent;');&#10;s = s.replace(/padding:\s*8px;/g, 'padding: 0;');&#10;fs.writeFileSync(file, s, 'utf8');&#10;console.log('Patched cms-image CSS: max-width/background/padding updated');&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>